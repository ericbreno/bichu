// Re-export do módulo para que chamadores importem da camada de tipos compartilhada.
export type {
  AnalyticsEvent,
  AnalyticsParams,
  AnalyticsProvider,
} from "@/modules/analytics/analytics.interface";
